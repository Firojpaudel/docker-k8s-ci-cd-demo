apiVersion: v1
kind: Service
metadata:
  name: workshop-service
spec:
  type: NodePort
  selector:
    app: workshop-app
  ports:
    - port: 80
      targetPort: 5000
      # This is the "Magic Port" we'll use to access the app locally
      nodePort: 30000
